% Initilization of struct
person = struct();
%to access struct fields
person.name = 'mohamed' ;
person.age = 23 ;
person.city = 'cairo' ;
%to display struct fields 

disp(person.name);
disp(person.age);
disp(person.city);